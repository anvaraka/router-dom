import { useLocation } from 'react-router-dom'

function Home() {
    /*  const { state } = useLocation() */
    return (
        <h1>This is my main Page {/* {state.name} {state.category} */}</h1>
    )
}

export default Home